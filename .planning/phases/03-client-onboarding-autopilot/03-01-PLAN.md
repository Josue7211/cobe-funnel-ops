---
phase: 03-client-onboarding-autopilot
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - src/App.tsx
  - src/api.ts
  - src/types.ts
autonomous: true
requirements:
  - ONB-01
  - ONB-02
must_haves:
  truths:
    - "Onboarding run records are visible in the console instead of only as a count."
    - "Folder, SOP, and invite assets are readable from the operator view."
    - "The onboarding proof path can be retried from the same live shell."
  artifacts:
    - path: src/App.tsx
      provides: "Onboarding state, proof panel, and retry trigger in the operator console"
    - path: src/api.ts
      provides: "Snapshot typing for onboarding runs"
    - path: src/types.ts
      provides: "Onboarding run type exposed to the frontend"
---

<objective>
Promote onboarding provisioning from a count to a visible proof surface in the live operator console.
</objective>

<execution_context>
@/home/josue/.codex/get-shit-done/workflows/execute-plan.md
@/home/josue/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-client-onboarding-autopilot/03-CONTEXT.md
@src/App.tsx
@src/api.ts
@src/types.ts
@server/sqlStore.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Thread onboarding runs through the client snapshot state</name>
  <files>src/api.ts, src/App.tsx, src/types.ts</files>
  <action>
    Extend the client snapshot typing and local state so onboarding runs are stored and can be rendered in the operator shell.
  </action>
  <acceptance_criteria>
    - Snapshot typing includes onboarding run records.
    - Onboarding run records survive bootstrap and provisioning refreshes.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add the onboarding proof panel to the metrics rail</name>
  <files>src/App.tsx, src/App.css</files>
  <action>
    Render onboarding status and asset URLs in the existing metrics or operations surface and keep the retry action reachable from the same shell.
  </action>
  <acceptance_criteria>
    - Folder, SOP, and invite assets are visible in the UI.
    - The operator can re-run onboarding provisioning from the proof surface.
  </acceptance_criteria>
</task>

</tasks>
