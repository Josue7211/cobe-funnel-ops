# 12-02: Tie Stages to Lifecycle Transitions

## Goal
Ensure the queue’s lane/stage badges reflect the same lifecycle milestones exposed in the graph.

## Depends on
- 12-01

## Requirements
- LIFE-02

## Success Criteria
1. Each queue row’s `lane` and `recommendAction` decodes from the same phase classification as the lifecycle nodes.
2. Stage badges (intake, consult, payment, onboarding, alert) update immediately when event-driven state changes.
3. Any new lifecycle nodes automatically refresh the queue filters so the operator can locate that lead fast.

## Plan
- Extend `classifyLane`/`recommendAction` to apply the lifecycle phase labels produced by the integration stream (intake→booking→payment→onboarding→alert).
- Surface the derived phase inside the queue item badges and the active workflow status block so the UI mirrors the graph vocabulary.
- Trigger a subtle queue pan/scroll update when the active lead changes so operators always see the node that just fired.
