---
phase: 02-ghl-call-routing-and-no-show-recovery
plan: 2
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - server/sqlStore.js
  - server/index.js
autonomous: true
requirements:
  - REC-01
  - REC-02
must_haves:
  truths:
    - "A no-show produces durable recovery work instead of a status-only update."
    - "Recovery and reschedule flows create delivery, audit, and timeline proof."
    - "Escalation semantics are visible and replayable."
  artifacts:
    - path: server/sqlStore.js
      provides: "Recovery queue side effects, attempts, and audit/timeline durability"
    - path: server/index.js
      provides: "Replayable workflow mutation contract for recovery flows"
---

<objective>
Build the durable recovery loop for no-show handling so the operator can trust and demonstrate the workflow from the console.
</objective>

<execution_context>
@/home/josue/.codex/get-shit-done/workflows/execute-plan.md
@/home/josue/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ghl-call-routing-and-no-show-recovery/02-CONTEXT.md
@server/sqlStore.js
@server/index.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create explicit no-show and recovery side effects</name>
  <files>server/sqlStore.js</files>
  <action>
    Expand the existing no-show path so it always creates delivery queue items, delivery attempts/history, audit events, and timeline-visible records for recovery and escalation behavior.
  </action>
  <acceptance_criteria>
    - Marking a lead as `no-show` creates durable recovery artifacts.
    - Recovery or reschedule actions update both booking and lead state consistently.
    - Recovery artifacts are visible to downstream reads without manual refresh hacks.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Support replayable recovery states through the API</name>
  <files>server/index.js, server/sqlStore.js</files>
  <action>
    Ensure the booking workflow contract can replay `booked`, `no-show`, `recovered`, and `rescheduled` flows without special-case manual steps.
  </action>
  <acceptance_criteria>
    - Recovery states can be exercised through the booking workflow endpoint.
    - Reschedule or recovered states do not leave orphaned queue or audit state behind.
  </acceptance_criteria>
</task>

</tasks>
