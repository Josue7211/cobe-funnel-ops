---
phase: 02-ghl-call-routing-and-no-show-recovery
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - server/sqlStore.js
  - server/index.js
  - src/types.ts
autonomous: true
requirements:
  - GHL-01
  - GHL-02
must_haves:
  truths:
    - "Booking state, lead stage, and owner assignment share one backend source of truth."
    - "Webhook-driven booking updates and operator-triggered recovery actions cannot diverge in stage semantics."
    - "Consult routing state is durable and visible to downstream queue, timeline, audit, and reporting reads."
  artifacts:
    - path: server/sqlStore.js
      provides: "Normalized booking pipeline logic and owner-aware state transitions"
    - path: server/index.js
      provides: "Stable booking workflow endpoint contract"
    - path: src/types.ts
      provides: "Frontend-visible types for consult, no-show, and recovery state"
---

<objective>
Normalize the consult-call pipeline in the backend so booked, no-show, recovery, and owner-routing behavior becomes explicit and reusable.
</objective>

<execution_context>
@/home/josue/.codex/get-shit-done/workflows/execute-plan.md
@/home/josue/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-ghl-call-routing-and-no-show-recovery/02-CONTEXT.md
@server/sqlStore.js
@server/index.js
@src/types.ts
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Make consult routing state explicit in the backend model</name>
  <files>server/sqlStore.js, src/types.ts</files>
  <action>
    Extend the existing booking and lead workflow logic so consult state is modeled explicitly enough for GHL-style routing. Ensure owner assignment, booking status, recovery action, and lead stage transitions are coherent and shared across manual actions and webhook paths.
  </action>
  <acceptance_criteria>
    - Lead stage mapping for consult workflows is defined in one place.
    - Owner assignment is durable in backend state rather than cosmetic UI-only state.
    - Booking updates for `booked`, `no-show`, `recovered`, and `rescheduled` are normalized.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Keep the API contract aligned with normalized backend behavior</name>
  <files>server/index.js</files>
  <action>
    Adjust booking workflow endpoint handling only where needed so the public contract cleanly supports the normalized pipeline behavior without introducing parallel routes.
  </action>
  <acceptance_criteria>
    - `/api/workflows/booking-update` remains the canonical booking mutation path.
    - Inputs required for owner routing and recovery are accepted and validated consistently.
  </acceptance_criteria>
</task>

</tasks>
