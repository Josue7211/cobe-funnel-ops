---
phase: 02-ghl-call-routing-and-no-show-recovery
plan: 3
type: execute
wave: 3
depends_on:
  - "02-01"
  - "02-02"
files_modified:
  - src/App.tsx
  - src/App.css
  - src/api.ts
autonomous: true
requirements:
  - GHL-02
  - REC-01
must_haves:
  truths:
    - "Call routing and no-show recovery are primary operator workflows in the UI."
    - "Owner assignment, call outcome, and recovery state are visible from queue and workspace surfaces."
    - "Recovery backlog and booked/no-show metrics are obvious to the operator."
  artifacts:
    - path: src/App.tsx
      provides: "Consult routing and recovery UX inside the existing shell"
    - path: src/App.css
      provides: "Reliable layout treatment for the new workflow surfaces"
    - path: src/api.ts
      provides: "Client helpers aligned to booking workflow behavior"
---

<objective>
Promote consult routing and no-show recovery to first-class operator UX inside the existing application shell.
</objective>

<execution_context>
@/home/josue/.codex/get-shit-done/workflows/execute-plan.md
@/home/josue/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ghl-call-routing-and-no-show-recovery/02-CONTEXT.md
@src/App.tsx
@src/App.css
@src/api.ts
@server/sqlStore.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Surface consult routing state in queue and workspace views</name>
  <files>src/App.tsx, src/api.ts</files>
  <action>
    Update the operator shell so owner assignment, booking status, consult outcome, and recovery backlog are visible in the queue, active workspace, and metrics surfaces using live backend state.
  </action>
  <acceptance_criteria>
    - Queue cards visibly reflect consult ownership and booking/recovery status.
    - Active workspace shows enough call-routing and recovery proof for a live walkthrough.
    - Metrics make booked, no-show, and recovered counts explicit.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Tighten layout and actions for recovery-first operator use</name>
  <files>src/App.css, src/App.tsx</files>
  <action>
    Refine the existing shell so routing and recovery actions feel primary and remain usable at common laptop widths, without redesigning the app.
  </action>
  <acceptance_criteria>
    - The new routing and recovery surfaces fit cleanly in the current shell.
    - No-show and recovery actions are reachable without UI collisions or hidden state.
  </acceptance_criteria>
</task>

</tasks>
