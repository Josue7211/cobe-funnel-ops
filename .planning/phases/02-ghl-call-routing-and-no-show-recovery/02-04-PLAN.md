---
phase: 02-ghl-call-routing-and-no-show-recovery
plan: 4
type: execute
wave: 4
depends_on:
  - "02-01"
  - "02-02"
  - "02-03"
files_modified:
  - server/http-smoke.mjs
  - server/smoke.mjs
  - src/App.tsx
autonomous: true
requirements:
  - REC-02
  - DEP-02
must_haves:
  truths:
    - "Booked, no-show, recovered, and rescheduled flows are replayable and verifiable."
    - "Core consult-call workflow regressions are detectable by automated smoke coverage."
    - "The operator UI exposes enough proof to demo the workflow without hidden backend steps."
  artifacts:
    - path: server/http-smoke.mjs
      provides: "HTTP-level coverage for booking and recovery flows"
    - path: server/smoke.mjs
      provides: "Backend scenario validation for consult-call workflow paths"
    - path: src/App.tsx
      provides: "Replayable proof surfaces and operator-visible validation"
---

<objective>
Add replay and verification coverage so the GHL routing and no-show recovery track can be demonstrated and trusted.
</objective>

<execution_context>
@/home/josue/.codex/get-shit-done/workflows/execute-plan.md
@/home/josue/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-ghl-call-routing-and-no-show-recovery/02-CONTEXT.md
@server/http-smoke.mjs
@server/smoke.mjs
@src/App.tsx
@server/sqlStore.js
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add booking and recovery smoke coverage</name>
  <files>server/http-smoke.mjs, server/smoke.mjs</files>
  <action>
    Extend the smoke harness so it covers booked, no-show, recovered, and rescheduled flows through the real workflow contracts.
  </action>
  <acceptance_criteria>
    - Booking and recovery flow success can be asserted without manual inspection.
    - A broken consult pipeline would fail automated smoke coverage.
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Add replayable proof in the operator shell</name>
  <files>src/App.tsx</files>
  <action>
    Ensure the operator shell can replay or clearly inspect the consult workflow states so the whole track can be demoed as a working product path.
  </action>
  <acceptance_criteria>
    - The operator can show booked, no-show, recovered, and rescheduled proof from the UI.
    - The consult workflow no longer depends on hidden backend steps during a demo.
  </acceptance_criteria>
</task>

</tasks>
