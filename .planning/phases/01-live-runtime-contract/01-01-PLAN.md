---
phase: 01-live-runtime-contract
plan: 1
type: execute
wave: 1
depends_on: []
files_modified:
  - server/index.js
  - server/http-smoke.mjs
  - package.json
autonomous: true
requirements:
  - DEP-01
  - DEP-03
must_haves:
  truths:
    - "The deployed browser can load the SPA and call /api/* from the same origin."
    - "Runtime smoke passes without relying on Vite's dev proxy or a configured Supabase mirror."
    - "Express is the documented runtime authority for the built app in Phase 1."
  artifacts:
    - path: server/index.js
      provides: "Express static hosting and SPA fallback that leaves /api/* routes intact"
    - path: server/http-smoke.mjs
      provides: "Production-contract smoke coverage for same-origin runtime and optional Supabase"
    - path: package.json
      provides: "Explicit production start command for the Node runtime"
  key_links:
    - from: server/index.js
      to: dist/index.html
      via: "Express static middleware and non-/api SPA fallback route"
      pattern: "express\\.static|index\\.html|dist"
    - from: package.json
      to: server/index.js
      via: "npm start or equivalent runtime script"
      pattern: "\"start\"\\s*:"
    - from: server/http-smoke.mjs
      to: server/index.js
      via: "spawned server process and HTTP assertions against built-app routes"
      pattern: "spawn\\(process\\.execPath, \\['server/index\\.js'\\]"
---

<objective>
Implement the production runtime contract for Phase 1 so the browser-visible product is one same-origin system instead of a static SPA plus hidden localhost proxy assumptions.

Purpose: Satisfy `DEP-01` and the deployment half of `DEP-03` with the minimum brownfield change: Express stays the runtime authority and serves the built Vite app plus `/api/*`.
Output: A startable Node runtime, same-origin static hosting in `server/index.js`, and smoke coverage that proves the contract without Supabase.
</objective>

<execution_context>
@/home/josue/.codex/get-shit-done/workflows/execute-plan.md
@/home/josue/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-live-runtime-contract/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/TESTING.md
@package.json
@server/index.js
@server/http-smoke.mjs
@vite.config.ts

<interfaces>
From `server/index.js` today:
```js
app.get('/api/health', (_request, response) => {
  response.json({ ok: true })
})

app.get('/api/realtime/stream', (request, response) => {
  attachRealtimeStream(request, response)
})
```

From `package.json` today:
```json
{
  "scripts": {
    "dev": "concurrently \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "node server/index.js",
    "build": "tsc -b && vite build",
    "test:http": "node server/http-smoke.mjs"
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the same-origin production server contract</name>
  <files>server/index.js, package.json</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/01-live-runtime-contract/01-RESEARCH.md
    package.json
    server/index.js
    vite.config.ts
  </read_first>
  <behavior>
    - Built frontend assets are served by Express from `dist/` on non-API routes.
    - `/api/*` endpoints continue to resolve to existing Express handlers unchanged.
    - The repo exposes a single Node start command for this same-origin runtime.
  </behavior>
  <action>Update `server/index.js` to serve `dist/` with `express.static(...)` and a non-`/api/` SPA fallback route per the Phase 1 research recommendation. Keep existing API and SSE routes authoritative and do not introduce a separate frontend server contract. Update `package.json` to add a production runtime script such as `start` that runs `node server/index.js`, because Phase 1 must document one deployable Node entrypoint instead of relying on `vite preview` or the dev proxy.</action>
  <acceptance_criteria>
    - `rg -n "express\\.static|index\\.html|dist" server/index.js` returns matches proving built-asset serving exists.
    - `rg -n "req\\.path\\.startsWith\\('/api/'\\)" server/index.js` returns a match proving the SPA fallback skips API routes.
    - `rg -n "\"start\"\\s*:" package.json` returns a production start script.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Express can serve the built SPA and `/api/*` from one process, and the repo exposes a production start command for that runtime.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rework HTTP smoke coverage around the real deployment contract</name>
  <files>server/http-smoke.mjs</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/01-live-runtime-contract/01-RESEARCH.md
    server/http-smoke.mjs
    server/index.js
    package.json
  </read_first>
  <behavior>
    - Smoke passes with no Supabase env vars present.
    - Smoke proves the built app is served from `/` and API routes still respond from the same process.
    - Smoke no longer treats remote sync success as a Phase 1 requirement.
  </behavior>
  <action>Refactor `server/http-smoke.mjs` so it first builds the frontend contract it is supposed to validate, then asserts the same-origin runtime directly: `/` returns the built HTML shell, `/api/health` responds from Express, and authenticated HTTP paths still work. Replace the current hard assumption that `syncStatus.configured === true` and `sync/push` always succeeds with config-sensitive assertions that keep Supabase optional in Phase 1. Preserve existing live workflow and SSE coverage while making the test describe the real deployment contract instead of the localhost-only proxy path.</action>
  <acceptance_criteria>
    - `rg -n "/api/health|/api/auth/login|/api/realtime/stream" server/http-smoke.mjs` returns matches for the existing live contract checks.
    - `rg -n "configured" server/http-smoke.mjs` returns a conditional sync-status assertion instead of a hardcoded configured-true expectation.
    - `npm run test:http` passes with Supabase environment variables unset.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:http</automated>
  </verify>
  <done>The smoke script proves the same-origin deployment contract and no longer fails Phase 1 when Supabase mirror config is absent.</done>
</task>

</tasks>

<verification>
Run `npm run build` and `npm run test:http`. Confirm the HTTP smoke now validates `/` plus `/api/*` from the same process and does not require Supabase configuration.
</verification>

<success_criteria>
`DEP-01` and the deployment half of `DEP-03` are satisfied by code, not docs alone: one Node runtime can serve the built SPA and API together, and the smoke suite proves that contract without localhost-only assumptions.
</success_criteria>

<output>
After completion, create `.planning/phases/01-live-runtime-contract/01-live-runtime-contract-01-SUMMARY.md`
</output>
