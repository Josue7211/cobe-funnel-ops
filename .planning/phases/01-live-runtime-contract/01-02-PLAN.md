---
phase: 01-live-runtime-contract
plan: 2
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - server/authSession.js
  - server/index.js
  - src/api.ts
  - src/App.tsx
  - server/http-smoke.mjs
  - .env.example
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - OPS-01
must_haves:
  truths:
    - "An operator can log in from the browser and the session survives normal page requests."
    - "An operator can log out from the browser and the HttpOnly session is cleared before any further protected request succeeds."
    - "Authenticated browser fetches and the SSE stream share the same browser session contract."
    - "Protected mutation endpoints reject unauthenticated access when the app is not in explicit local-dev bypass mode."
    - "After a valid login, the queue, active workflow, operations rail, and reports load from backend data instead of unauthenticated seeded fallback state."
    - "If auth bootstrap or the live runtime breaks, the console stays honest by showing auth-required or degraded state instead of pretending the authenticated product loaded."
  artifacts:
    - path: server/authSession.js
      provides: "Cookie-capable admin session helpers and non-local secret enforcement"
    - path: server/index.js
      provides: "Login/session/logout routes and `requireAdmin` enforcement for browser cookies"
    - path: src/api.ts
      provides: "Credential-aware client request helpers, auth endpoints, and backend-backed console loaders"
    - path: src/App.tsx
      provides: "Frontend login/session gate plus authenticated queue/workflow/operations-rail/report bootstrap"
    - path: server/http-smoke.mjs
      provides: "Assertions for authenticated success and unauthenticated rejection"
  key_links:
    - from: server/index.js
      to: server/authSession.js
      via: "login and requireAdmin session verification"
      pattern: "createAdminSessionToken|verifyAdminSessionToken"
    - from: src/api.ts
      to: server/index.js
      via: "fetch with credentials to /api/auth/* and protected routes"
      pattern: "credentials:\\s*'include'|/api/auth/login|/api/auth/session"
    - from: src/App.tsx
      to: src/api.ts
      via: "session-guarded bootstrap of queue, workflow shell, operations rail, and reports"
      pattern: "fetchBootstrap|fetchQueue|fetchReportsOverview|auth"
    - from: src/App.tsx
      to: /api/realtime/stream
      via: "EventSource using same-origin browser cookies after session bootstrap"
      pattern: "new EventSource\\('/api/realtime/stream'\\)"
---

<objective>
Wire the browser auth contract for the real operator runtime so frontend login, protected mutations, and SSE all use one same-origin session boundary.

Purpose: Satisfy `AUTH-01`, `AUTH-02`, and the auth-dependent portion of `OPS-01` without introducing cross-origin token complexity.
Output: Cookie-backed browser auth, explicit local-dev bypass semantics, and smoke-tested rejection of unauthenticated mutation traffic.
</objective>

<execution_context>
@/home/josue/.codex/get-shit-done/workflows/execute-plan.md
@/home/josue/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-live-runtime-contract/01-VALIDATION.md
@.planning/phases/01-live-runtime-contract/01-RESEARCH.md
@.planning/codebase/ARCHITECTURE.md
@.planning/codebase/CONCERNS.md
@.planning/codebase/CONVENTIONS.md
@server/authSession.js
@server/index.js
@src/api.ts
@src/App.tsx
@server/http-smoke.mjs
@.env.example

<interfaces>
From `server/authSession.js` today:
```js
export function authenticateAdmin(username, password) {}
export function createAdminSessionToken(username = defaultUser) {}
export function verifyAdminSessionToken(token) {}
```

From `src/api.ts` today:
```ts
async function request(path: string, init?: RequestInit) {}
export async function fetchBootstrap() {}
export async function fetchQueue(filters?: {...}) {}
```

From `server/index.js` today:
```js
app.post('/api/auth/login', ...)
app.get('/api/auth/session', ...)
app.post('/api/auth/logout', ...)
function requireAdmin(request, response, next) {}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Convert backend auth to a browser-safe cookie session contract</name>
  <files>server/authSession.js, server/index.js, .env.example, server/http-smoke.mjs</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/01-live-runtime-contract/01-RESEARCH.md
    .env.example
    server/authSession.js
    server/index.js
    server/http-smoke.mjs
  </read_first>
  <behavior>
    - Successful login sets an `HttpOnly` session cookie and `/api/auth/session` can read it.
    - Logout clears the same cookie and subsequent protected requests fail until the next successful login.
    - `requireAdmin` accepts the browser cookie and still keeps optional bearer/header support for smoke scripts or manual API clients.
    - Unauthenticated mutation requests return `401` when non-local protection is enabled; the dev bypass is explicit and named.
  </behavior>
  <action>Extend `server/authSession.js` with helpers for reading/writing a named admin session cookie and for checking whether the runtime is allowed to bypass auth locally. Update `server/index.js` so `/api/auth/login` sets the cookie, `/api/auth/logout` is mandatory and clears it cleanly with the same cookie contract, and `/api/auth/session` plus `requireAdmin` accept the cookie as the primary browser credential. Replace the current `if (!configuredToken) return next()` behavior with an explicit local-dev allowance controlled by environment, because `AUTH-02` requires protected mutations to fail closed outside deliberate local use. Update `.env.example` to document the new auth flags and extend `server/http-smoke.mjs` with positive login/session/logout assertions plus a post-logout unauthenticated protected-route assertion.</action>
  <acceptance_criteria>
    - `rg -n "Set-Cookie|setHeader\\(|cookie" server/index.js server/authSession.js` returns matches showing cookie session support exists.
    - `rg -n "/api/auth/logout|logout" server/index.js server/http-smoke.mjs src/api.ts` returns matches showing logout is part of the contract and smoke path.
    - `rg -n "401" server/http-smoke.mjs` returns an unauthenticated protected-route assertion.
    - `rg -n "ALLOW_LOCAL|LOCAL_DEV|BYPASS" .env.example server/authSession.js server/index.js` returns the explicit local bypass contract.
    - `npm run test:http` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:http</automated>
  </verify>
  <done>Backend login issues a browser-usable session cookie, logout clears that cookie, protected mutations fail for unauthenticated callers outside the named local-dev allowance, and smoke coverage proves login, logout, and post-logout rejection paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire the React client to the real session and gate the console on it</name>
  <files>src/api.ts, src/App.tsx</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/01-live-runtime-contract/01-RESEARCH.md
    src/api.ts
    src/App.tsx
    server/index.js
    server/authSession.js
  </read_first>
  <behavior>
    - Client requests include browser credentials by default.
    - The UI can submit username/password to `/api/auth/login`, bootstrap the active session from `/api/auth/session`, and clear state on logout.
    - The main operator console only initializes live queue, active workflow shell, operations rail, reports, and `EventSource('/api/realtime/stream')` after a valid session is present.
    - Logging in after an auth-required state starts the live console and realtime stream, and logging out tears them down so reconnect only happens after the next successful session bootstrap.
    - If session bootstrap, authenticated reads, or runtime calls fail, the authenticated console path shows an explicit auth-required or degraded state instead of masquerading as a healthy live load.
  </behavior>
  <action>Refactor `src/api.ts` so the shared `request(...)` helper uses `credentials: 'include'`, adds typed helpers for login/session/logout, and preserves current relative `/api/*` paths. Update `src/App.tsx` to introduce minimal auth state for Phase 1: a login screen or gate, session bootstrap on mount, logout handling, and conditional activation of the existing queue, active workflow shell, operations rail, and reports fetches only after session verification succeeds, per `OPS-01`. Gate creation of `EventSource('/api/realtime/stream')` on successful session bootstrap/login instead of starting it speculatively before auth settles, and ensure logout closes the existing stream plus prevents reconnect until the next successful login. When auth bootstrap or authenticated runtime reads fail, surface an explicit auth-required or degraded state for the current console shell instead of letting unauthenticated seeded state masquerade as the live product. Keep the UI scope tight per the phase constraint: do not redesign the dashboard; add only the auth contract needed for the existing console to function honestly.</action>
  <acceptance_criteria>
    - `rg -n "credentials:\\s*'include'" src/api.ts` returns a match.
    - `rg -n "/api/auth/login|/api/auth/session|/api/auth/logout" src/api.ts` returns auth helper usage.
    - `rg -n "username|password|logout|session" src/App.tsx` returns the new auth gate/session state.
    - `rg -n "fetchBootstrap|fetchQueue|fetchReportsOverview" src/App.tsx` returns authenticated console-loading calls wired through the post-login path.
    - `rg -n "degraded|auth_required|runtimeStatus|liveStatus|apiError" src/App.tsx` returns explicit failure-state handling for the authenticated console path.
    - `rg -n "new EventSource\\('/api/realtime/stream'\\)|close\\(\\)" src/App.tsx` returns explicit realtime stream start/teardown handling tied to auth state.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <manual>Start the app, load `/` with no valid session, and confirm the login/auth-required gate appears before queue, active workflow, operations rail, reports, or realtime UI initializes. Log in and confirm those console surfaces populate from the backend and the realtime stream starts only after session bootstrap. Then force an auth/runtime failure after login and confirm the shell shows an explicit auth-required or degraded state instead of pretending the authenticated console is still healthy. Log out and confirm the console returns to the auth gate, the previous live state is cleared, and the realtime connection is torn down until the next successful login.</manual>
  </verify>
  <done>The browser app can log in, maintain session-backed authenticated requests, and only expose the live queue, active workflow, operations rail, reports, and realtime stream after session bootstrap succeeds; auth/runtime failures stay visible instead of being masked, and logout clears protected state until the next login.</done>
</task>

</tasks>

<verification>
Run `npm run test:http` to prove cookie login plus unauthenticated rejection. Run `npm run build` to prove the React auth gate compiles cleanly against the updated API contract.
</verification>

<success_criteria>
`AUTH-01` and `AUTH-02` are satisfied through the real browser path, and `OPS-01` is explicit for the authenticated console contract: the operator can log in from the frontend, protected mutations require a real session outside explicit local bypass mode, the same-origin session also supports SSE, and the live queue/workflow/operations-rail/report surfaces only load after successful auth while failure states remain honest.
</success_criteria>

<output>
After completion, create `.planning/phases/01-live-runtime-contract/01-live-runtime-contract-02-SUMMARY.md`
</output>
