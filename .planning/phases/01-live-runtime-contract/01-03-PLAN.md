---
phase: 01-live-runtime-contract
plan: 3
type: execute
wave: 3
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - src/App.tsx
  - vite.config.ts
  - README.md
autonomous: true
requirements:
  - OPS-01
  - DEP-03
must_haves:
  truths:
    - "The authenticated operator console no longer pretends the live product is healthy when API/auth/runtime calls fail."
    - "Queue, active workflow, operations rail, and report surfaces stay visibly tied to authenticated backend truth instead of silent fallback behavior."
    - "The supported deployment contract is documented as one same-origin Node runtime, not a static-only site."
  artifacts:
    - path: src/App.tsx
      provides: "Explicit live/degraded/auth-required state instead of silent seeded fallback across the console shell"
    - path: vite.config.ts
      provides: "Build settings that match the same-origin deployment contract"
    - path: README.md
      provides: "Operator-facing runtime and deployment instructions for the supported topology"
  key_links:
    - from: src/App.tsx
      to: src/api.ts
      via: "error handling that surfaces failed authenticated fetch/auth states for queue/workflow/rail/report reads"
      pattern: "catch|set.*degrad|set.*error|set.*auth"
    - from: vite.config.ts
      to: server/index.js
      via: "build output intended for same-origin serving instead of embedded static hosting assumptions"
      pattern: "base:"
---

<objective>
Remove the fake-live deployment behavior and document the honest Phase 1 runtime contract so operators exercise the same product path the repo now supports.

Purpose: Finish `DEP-03` and the live-integrity portion of `OPS-01` by surfacing failures instead of masking them and by documenting one supported topology.
Output: Explicit degraded/live handling in the UI, same-origin-aligned build config, and docs that describe the real contract.
</objective>

<execution_context>
@/home/josue/.codex/get-shit-done/workflows/execute-plan.md
@/home/josue/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-live-runtime-contract/01-VALIDATION.md
@.planning/phases/01-live-runtime-contract/01-RESEARCH.md
@.planning/codebase/CONCERNS.md
@src/App.tsx
@src/api.ts
@vite.config.ts
@README.md
@server/http-smoke.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace silent frontend fallback with explicit live/degraded runtime state</name>
  <files>src/App.tsx</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/01-live-runtime-contract/01-RESEARCH.md
    .planning/codebase/CONCERNS.md
    src/App.tsx
    src/api.ts
  </read_first>
  <behavior>
    - Failed bootstrap, queue, workflow-shell, operations-rail, timeline, or reports calls no longer silently substitute seeded data as if the authenticated live runtime succeeded.
    - Failed live mutations in `src/App.tsx` no longer leave optimistic or locally replayed success state visible as if the backend accepted the action.
    - The operator sees explicit live/degraded or auth-failure status in the existing UI shell around the queue, active workflow, operations rail, and reports surfaces.
    - Existing local demo structures can remain as non-live helpers, but they are visually and behaviorally separated from the live runtime path.
  </behavior>
  <action>Refactor the `useEffect` fetch paths and the live action/mutation handlers in `src/App.tsx` so Phase 1 surfaces authenticated runtime truth instead of silently absorbing errors. Remove the current silent `.catch(() => {})` and seeded queue/timeline fallbacks from the live read path, replacing them with explicit state such as `live`, `degraded`, `auth_required`, or a similar concrete status that the UI renders near the existing console shell. Apply this honesty requirement to the queue, active workflow shell, operations rail, and reports surfaces so `OPS-01` remains tied to backend data after auth instead of drifting back to seeded stand-ins. Also audit local state writes triggered by live mutations in `src/App.tsx`: if a protected action fails because auth/runtime is unavailable, the UI must not keep the locally mutated queue, workflow, report, or timeline state as though the live backend succeeded. Any remaining demo-only helpers must be clearly isolated from the authenticated live path. Preserve the existing dashboard structure; do not redesign the interface. The goal is to stop fake deployment success, not to rebuild the product surface.</action>
  <acceptance_criteria>
    - `rg -n "degraded|auth_required|liveStatus|runtimeStatus|apiError" src/App.tsx` returns explicit runtime-state handling.
    - `rg -n "Keep the seeded frontend state when the API is unavailable" src/App.tsx` returns no matches.
    - `rg -n "setQueueRecords\\(fallback\\)|eventLog" src/App.tsx` shows fallback-only paths removed or clearly isolated from the live contract.
    - `rg -n "reportsOverview|queueRecords|activeLead|connectors" src/App.tsx` shows the existing console surfaces are driven through the explicit live/auth state instead of unconditional fallback load paths.
    - `rg -n "catch|set.*Queue|set.*Workflow|set.*Timeline|set.*Report" src/App.tsx` shows failed live action paths do not leave successful local mutation state in place without an explicit degraded/error branch.
    - `npm run build` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
    <manual>Run the app with the Phase 1 auth/runtime contract, clear browser cookies, and load `/`. Confirm the UI shows an explicit auth-required or equivalent runtime status instead of rendering seeded queue, active workflow, operations rail, or report data as if the authenticated console loaded. Then force a failed live bootstrap or API request after login and confirm the existing shell shows a visible degraded/live-error state while those surfaces do not masquerade as healthy backend-backed data. Finally, trigger at least one protected mutation in `src/App.tsx` with an expired or cleared session and confirm the UI does not retain the local post-action queue/workflow/report state as though the backend accepted the change.</manual>
  </verify>
  <done>The operator UI no longer masquerades as a healthy live deployment when backend/auth calls fail, and failed live mutations cannot leave successful-looking local state behind in the existing shell.</done>
</task>

<task type="auto">
  <name>Task 2: Align build and docs to the one supported runtime topology</name>
  <files>vite.config.ts, README.md</files>
  <read_first>
    .planning/ROADMAP.md
    .planning/phases/01-live-runtime-contract/01-RESEARCH.md
    vite.config.ts
    README.md
    package.json
    server/http-smoke.mjs
  </read_first>
  <action>Update `vite.config.ts` so the build output matches the same-origin deployment contract instead of static-host assumptions such as `base: './'`. Rewrite the README live-demo/deployment sections to state that Phase 1 supports one Node-capable same-origin deployment where Express serves the built app and `/api/*`, and remove GitHub Pages or static-only positioning as the primary product path. Keep CI smoke hardening out of this Phase 1 plan because roadmap ownership for that work sits in `DEP-02` / Phase 4; this task is limited to the honest deployment contract required for `DEP-03` now.</action>
  <acceptance_criteria>
    - `rg -n "base:\\s*'/'|base:\\s*\"/\"" vite.config.ts` returns a same-origin-aligned base path.
    - `rg -n "same-origin|Express|Node|/api" README.md` returns the documented supported topology.
    - `rg -n "GitHub Pages|static-only|local proxy" README.md` shows those assumptions removed or clearly demoted from the supported product path.
  </acceptance_criteria>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>Docs and build config describe the same supported runtime contract instead of preserving static-only deployment assumptions, without pulling later CI hardening scope into Phase 1.</done>
</task>

</tasks>

<verification>
Run `npm run build`. Confirm the README and Vite config now point to one same-origin Express runtime, and the UI surfaces degraded/auth failure instead of silently pretending the backend is fine.
</verification>

<success_criteria>
Phase 1 is honest end to end: operators see a real authenticated live runtime state, the queue/workflow/operations-rail/report shell does not pretend to be healthy when auth/runtime breaks, and the repo no longer advertises or depends on static-only deployment assumptions for the working product path.
</success_criteria>

<output>
After completion, create `.planning/phases/01-live-runtime-contract/01-live-runtime-contract-03-SUMMARY.md`
</output>
